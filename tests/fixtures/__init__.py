"""Static fixture data for integration tests.

Tests now read directly from the files under ``湖北大学配置/`` instead of
regenerating sample data at runtime. Place production sample data in this
directory and the test suite will operate on it unchanged.
"""
